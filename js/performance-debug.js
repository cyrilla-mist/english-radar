(function () {
  'use strict';
  var search = window.location && window.location.search ? window.location.search : '';
  if (search.indexOf('debug=performance') === -1) return;
  var startedAt = window.performance && typeof window.performance.timeOrigin === 'number' ? window.performance.timeOrigin : Date.now();
  var records = []; var calls = {}; var signalCounts = { active: null, imported: null }; var now = window.performance && typeof window.performance.now === 'function' ? function () { return window.performance.now(); } : function () { return Date.now(); };
  function clean(value) { return Number(Number(value).toFixed(2)); }
  function record(name, elapsed, details) { records.push(Object.assign({ name: name, ms: clean(elapsed) }, details || {})); }
  function measure(name, callback) { var started = now(); var result = callback(); record(name, now() - started); return result; }
  function increment(name) { calls[name] = (calls[name] || 0) + 1; return calls[name]; }
  function wrap(object, method, label, resultHandler) { if (!object || typeof object[method] !== 'function') return; var original = object[method]; object[method] = function () { var call = increment(label); var started = now(); var result = original.apply(this, arguments); var elapsed = now() - started; var details = { call: call }; if (resultHandler) resultHandler(result, details, elapsed); record(label, elapsed, details); return result; }; }
  function sum(names) { return clean(records.filter(function (item) { return names.indexOf(item.name) !== -1; }).reduce(function (total, item) { return total + item.ms; }, 0)); }
  function first(name) { var item = records.find(function (entry) { return entry.name === name; }); return item ? item.ms : 0; }
  function duplicateWarnings() { return ['storage.getCustomSignals', 'content.getImportedSignals', 'content.getActiveLearningSignals'].filter(function (name) { return (calls[name] || 0) > 1; }); }
  function snapshot() {
    var nav = window.__englishRadarNavigationPerformance && window.__englishRadarNavigationPerformance.snapshot ? window.__englishRadarNavigationPerformance.snapshot() : {};
    var longest = records.reduce(function (max, item) { return Math.max(max, item.ms); }, 0);
    return { page: (window.location.pathname || '').split('/').pop() || 'index.html', totalInitializationMs: clean((window.__englishRadarNavigationPerformance ? window.__englishRadarNavigationPerformance.now() : Date.now()) - startedAt), storageReadMs: sum(['storage.getCustomSignals', 'storage.getProgress', 'storage.getQuizHistory', 'storage.getInbox', 'storage.getSyncHistory', 'storage.getSettings', 'storage.getCurrentSession']), jsonParseMs: sum(['JSON.parse']), registryInitMs: first('contentRegistry.init'), importedNormalizeCopyMs: sum(['content.importedSignals.normalizeCopy', 'registry.importedValidationFilter']), activeSignalsBuildMs: sum(['content.activeSignals.build', 'registry.activeConcatDedupe']), progressHistoryIndexingMs: sum(['storage.getProgress', 'storage.getQuizHistory', 'storage.getInbox', 'storage.getSyncHistory']), pagePreparationMs: sum(['today.dailyMix', 'today.dashboardStatistics', 'dictionary.index', 'dictionary.searchIndex', 'dictionary.categoryCounts', 'quiz.candidateSignals', 'quiz.distractorPools', 'quiz.generation', 'quiz.sessionSelection', 'me.masteryStatistics', 'me.categoryCoverage', 'me.packMetadata', 'me.recentActivity']), domRenderMs: sum(['today.render', 'today.dashboardDomRender', 'dictionary.initial50.render', 'dictionary.styleLayout', 'quiz.DOM render', 'me.render']), initialDomNodes: document.getElementsByTagName('*').length, longestSynchronousTaskMs: clean(longest), activeSignalsCount: signalCounts.active, importedSignalsCount: signalCounts.imported, callCounts: Object.assign({}, calls), duplicateCallWarnings: duplicateWarnings(), navigation: nav.navigation || {}, paint: nav.paint || {}, firstUsableContentMs: nav.firstUsableContent, totalBlockingTimeMs: nav.totalBlockingTime || 0, longTasksOver50ms: nav.longTasksOver50ms || 0, longTasksOver100ms: nav.longTasksOver100ms || 0, longestObservedTask: nav.longestTask || { start: 0, duration: 0 }, layoutShift: nav.layoutShift || 0, interactions: nav.interactions || [], resources: nav.resources || [], stageRecords: records.slice() };
  }
  function reportRows(payload) { var nav = payload.navigation; var paint = payload.paint; return [['Page', payload.page], ['Total initialization', payload.totalInitializationMs + ' ms'], ['Storage read', payload.storageReadMs + ' ms'], ['JSON.parse', payload.jsonParseMs + ' ms'], ['Content Registry init', payload.registryInitMs + ' ms'], ['Imported Signals normalize/copy', payload.importedNormalizeCopyMs + ' ms'], ['Active Signals build', payload.activeSignalsBuildMs + ' ms'], ['Progress/history indexing', payload.progressHistoryIndexingMs + ' ms'], ['Page-specific data preparation', payload.pagePreparationMs + ' ms'], ['DOM render', payload.domRenderMs + ' ms'], ['Initial DOM node count', payload.initialDomNodes], ['DNS', Number(nav.dns || 0).toFixed(2) + ' ms'], ['Connection', Number(nav.connection || 0).toFixed(2) + ' ms'], ['Request / response', Number(nav.requestResponse || 0).toFixed(2) + ' ms'], ['DOM interactive', Number(nav.domInteractive || 0).toFixed(2) + ' ms'], ['DOMContentLoaded', Number(nav.domContentLoaded || 0).toFixed(2) + ' ms'], ['Window load', Number(nav.load || 0).toFixed(2) + ' ms'], ['First Paint', paint.firstPaint === null ? '—' : Number(paint.firstPaint).toFixed(2) + ' ms'], ['First Contentful Paint', paint.firstContentfulPaint === null ? '—' : Number(paint.firstContentfulPaint).toFixed(2) + ' ms'], ['Time to first usable content', payload.firstUsableContentMs === null ? '—' : Number(payload.firstUsableContentMs).toFixed(2) + ' ms'], ['Total Blocking Time', Number(payload.totalBlockingTimeMs).toFixed(2) + ' ms'], ['Long Tasks >50ms', payload.longTasksOver50ms], ['Long Tasks >100ms', payload.longTasksOver100ms], ['Longest observed task', Number(payload.longestObservedTask.duration || 0).toFixed(2) + ' ms @ ' + Number(payload.longestObservedTask.start || 0).toFixed(2) + ' ms'], ['Layout Shift', payload.layoutShift], ['Interactions captured', payload.interactions.length], ['Active Signals count', payload.activeSignalsCount === null ? '—' : payload.activeSignalsCount], ['Imported Signals count', payload.importedSignalsCount === null ? '—' : payload.importedSignalsCount], ['Duplicate call warning', payload.duplicateCallWarnings.length ? payload.duplicateCallWarnings.join(', ') : 'None']]; }
  function addCell(row, value) { var cell = document.createElement('td'); cell.textContent = String(value); row.appendChild(cell); }
  function ensurePanel() { var panel = document.querySelector('[data-performance-panel]'); if (panel) return panel; panel = document.createElement('aside'); panel.setAttribute('data-performance-panel', 'true'); panel.setAttribute('aria-live', 'polite'); panel.style.cssText = 'position:fixed;z-index:9999;left:12px;right:12px;bottom:12px;max-height:44vh;overflow:auto;padding:12px 14px;background:#18231f;color:#f3f5ed;border:1px solid rgba(255,255,255,.24);box-shadow:0 8px 30px rgba(0,0,0,.28);font:12px/1.45 system-ui,sans-serif;'; document.body.appendChild(panel); return panel; }
  function reportText(payload) { return JSON.stringify(payload, null, 2); }
  function setStatus(panel, message) { var status = panel.querySelector('[data-performance-status]'); if (status) status.textContent = message; }
  function showRawReport(panel, payload, expanded) { var raw = panel.querySelector('[data-performance-raw]'); var toggle = panel.querySelector('[data-performance-raw-toggle]'); if (!raw || !toggle) return; raw.value = reportText(payload); raw.hidden = !expanded; toggle.textContent = expanded ? 'Hide raw report' : 'Show raw report'; }
  function copyWithTextarea(text) { var area = document.createElement('textarea'); area.value = text; area.setAttribute('readonly', ''); area.style.cssText = 'position:fixed;left:-9999px;top:0;width:1px;height:1px;opacity:0;'; document.body.appendChild(area); area.focus(); area.select(); var copied = false; try { copied = !!document.execCommand && document.execCommand('copy'); } catch (error) { copied = false; } if (area.parentNode) area.parentNode.removeChild(area); return copied; }
  async function copyReport(panel, payload) { var text = reportText(payload); if (window.navigator && window.navigator.clipboard && typeof window.navigator.clipboard.writeText === 'function') { try { await window.navigator.clipboard.writeText(text); setStatus(panel, 'Report copied.'); return true; } catch (error) { /* fallback */ } } if (copyWithTextarea(text)) { setStatus(panel, 'Report copied.'); return true; } showRawReport(panel, payload, true); setStatus(panel, document.execCommand ? 'Copy failed. Download the report instead.' : 'Clipboard unavailable. Use “Show raw report”.'); return false; }
  function downloadReport(panel, payload) { var text = reportText(payload); var blob = new Blob([text], { type: 'application/json' }); var url = URL.createObjectURL(blob); var link = document.createElement('a'); var page = String(payload.page || 'page').replace(/[^a-z0-9_-]+/gi, '-').replace(/^-+|-+$/g, '').toLowerCase() || 'page'; var stamp = new Date().toISOString().replace(/[-:]/g, '').replace(/\.\d{3}Z$/, 'Z'); link.href = url; link.download = 'english-radar-performance-' + page + '-' + stamp + '.json'; document.body.appendChild(link); link.click(); link.remove(); URL.revokeObjectURL(url); setStatus(panel, 'Report downloaded.'); }
  function render(payload) { var panel = ensurePanel(); panel.textContent = ''; var heading = document.createElement('div'); heading.style.cssText = 'display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;margin-bottom:8px;'; var title = document.createElement('strong'); title.textContent = 'Performance Report / ' + payload.page; heading.appendChild(title); var actions = document.createElement('span'); actions.style.cssText = 'display:flex;flex-wrap:wrap;gap:6px;'; [['Copy report', function () { copyReport(panel, payload); }], ['Show raw report', function () { var raw = panel.querySelector('[data-performance-raw]'); showRawReport(panel, payload, !!raw.hidden); }], ['Download report', function () { downloadReport(panel, payload); }], ['Refresh measurement', function () { render(snapshot()); }]].forEach(function (item) { var button = document.createElement('button'); button.type = 'button'; button.textContent = item[0]; if (item[0] === 'Show raw report') button.setAttribute('data-performance-raw-toggle', 'true'); button.style.cssText = 'padding:5px 8px;border:1px solid rgba(255,255,255,.28);background:#cedcc7;color:#18231f;cursor:pointer;font:11px system-ui,sans-serif;'; button.addEventListener('click', item[1]); actions.appendChild(button); }); heading.appendChild(actions); panel.appendChild(heading); var status = document.createElement('div'); status.setAttribute('data-performance-status', 'true'); status.style.cssText = 'min-height:18px;margin:4px 0;color:#9eb09b;'; panel.appendChild(status); var raw = document.createElement('textarea'); raw.setAttribute('data-performance-raw', 'true'); raw.setAttribute('readonly', ''); raw.hidden = true; raw.value = reportText(payload); raw.style.cssText = 'display:block;width:100%;min-height:180px;box-sizing:border-box;margin:6px 0;padding:8px;background:#f3f5ed;color:#202521;border:1px solid #cad1c7;font:11px/1.4 ui-monospace,monospace;white-space:pre;overflow:auto;'; panel.appendChild(raw); var table = document.createElement('table'); table.style.cssText = 'width:100%;border-collapse:collapse;'; reportRows(payload).forEach(function (item) { var row = document.createElement('tr'); var label = document.createElement('th'); label.textContent = item[0]; label.style.cssText = 'padding:3px 8px 3px 0;text-align:left;color:#cedcc7;font-weight:600;'; row.appendChild(label); addCell(row, item[1]); row.lastChild.style.cssText = 'padding:3px 0;text-align:right;color:#f5f2e8;'; table.appendChild(row); }); panel.appendChild(table); var counts = document.createElement('p'); counts.textContent = 'Calls: ' + Object.keys(payload.callCounts).map(function (name) { return name + '=' + payload.callCounts[name]; }).join(' / '); counts.style.cssText = 'margin:8px 0 0;color:#9eb09b;word-break:break-word;'; panel.appendChild(counts); var interactions = document.createElement('p'); interactions.textContent = 'Interaction samples: ' + (payload.interactions.length ? payload.interactions.map(function (item) { return item.type + ' ' + item.duration + 'ms'; }).join(' / ') : 'None yet.'); interactions.style.cssText = 'margin:4px 0 0;color:#9eb09b;word-break:break-word;'; panel.appendChild(interactions); var resourceTitle = document.createElement('strong'); resourceTitle.textContent = 'Resources (' + payload.resources.length + ')'; resourceTitle.style.cssText = 'display:block;margin-top:8px;color:#cedcc7;'; panel.appendChild(resourceTitle); var resourceTable = document.createElement('table'); resourceTable.style.cssText = 'width:100%;border-collapse:collapse;'; payload.resources.forEach(function (item) { var row = document.createElement('tr'); addCell(row, item.file); addCell(row, item.transferSize + ' B'); addCell(row, item.decodedBodySize + ' B'); addCell(row, item.duration + ' ms'); resourceTable.appendChild(row); }); panel.appendChild(resourceTable); var stageTitle = document.createElement('strong'); stageTitle.textContent = 'Stage details'; stageTitle.style.cssText = 'display:block;margin-top:8px;color:#cedcc7;'; panel.appendChild(stageTitle); var stageTable = document.createElement('table'); stageTable.style.cssText = 'width:100%;border-collapse:collapse;'; payload.stageRecords.forEach(function (item) { var row = document.createElement('tr'); addCell(row, item.name); addCell(row, item.ms + ' ms'); stageTable.appendChild(row); }); panel.appendChild(stageTable); }
  function publish() { var payload = snapshot(); window.__englishRadarPerformance = payload; render(payload); console.info('[English Radar performance]', payload); }
  var publishScheduled = false; function schedulePublish() { if (publishScheduled) return; publishScheduled = true; var raf = window.requestAnimationFrame || function (callback) { callback(); }; raf(function () { raf(function () { publishScheduled = false; publish(); }); }); }
  window.EnglishRadarPerformanceDebug = { enabled: true, measure: measure, record: record, increment: increment, snapshot: snapshot, publish: publish, copyReport: copyReport, downloadReport: downloadReport };
  wrap(window.EnglishRadarStorage, 'getCustomSignals', 'storage.getCustomSignals', function (result) { if (signalCounts.imported === null && result && result.signals && typeof result.signals === 'object') signalCounts.imported = Object.keys(result.signals).length; }); wrap(window.EnglishRadarStorage, 'getProgress', 'storage.getProgress'); wrap(window.EnglishRadarStorage, 'getQuizHistory', 'storage.getQuizHistory'); wrap(window.EnglishRadarStorage, 'getInbox', 'storage.getInbox'); wrap(window.EnglishRadarStorage, 'getSyncHistory', 'storage.getSyncHistory'); wrap(window.EnglishRadarStorage, 'getSettings', 'storage.getSettings'); wrap(window.EnglishRadarStorage, 'getCurrentSession', 'storage.getCurrentSession'); wrap(window.EnglishRadarContent, 'getImportedSignals', 'content.getImportedSignals', function (result, details, elapsed) { signalCounts.imported = Array.isArray(result) ? result.length : null; details.count = signalCounts.imported; record('content.importedSignals.normalizeCopy', elapsed); }); wrap(window.EnglishRadarContent, 'getActiveLearningSignals', 'content.getActiveLearningSignals', function (result, details, elapsed) { signalCounts.active = Array.isArray(result) ? result.length : null; details.count = signalCounts.active; if (details.call === 1) { record('contentRegistry.init', elapsed); record('content.activeSignals.build', elapsed); } }); wrap(window.EnglishRadarContent, 'getDictionarySignals', 'content.getDictionarySignals', function (result, details) { details.count = Array.isArray(result) ? result.length : null; }); wrap(window.EnglishRadarContent, 'getSignalSource', 'content.getSignalSource');
  window.addEventListener('load', schedulePublish); document.addEventListener('click', schedulePublish, true); document.addEventListener('input', schedulePublish, true); window.addEventListener('scroll', schedulePublish, { passive: true });
}());

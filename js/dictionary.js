(function () {
  'use strict';
  var registry = window.EnglishRadarContent; var storage = window.EnglishRadarStorage; var progress = storage ? storage.getProgress() : {}; var view = 'community'; var filter = 'all'; var sourceFilter = 'all'; var query = ''; var visibleCount = 50; var batchSize = 50; var searchTimer = null;
  var categories = ['Internet Culture', 'AI Builder', 'GitHub', 'GitHub / Development', 'Product Design', 'Fandom', 'Sports', 'Sports / Everyday', 'Everyday English', 'Personal']; var list = document.querySelector('[data-dictionary-list]'); var empty = document.querySelector('[data-dictionary-empty]'); var filterArea = document.querySelector('[data-dictionary-filters]'); var sourceArea = document.querySelector('[data-dictionary-sources]'); var search = document.querySelector('#dictionary-search'); var loadMore = document.querySelector('[data-dict-load-more]');
  var signals = registry ? registry.getDictionarySignals() : (Array.isArray(window.ENGLISH_RADAR_SIGNALS) ? window.ENGLISH_RADAR_SIGNALS : []); var debug = window.EnglishRadarPerformanceDebug; var buildEntries = function () { return signals.map(function (signal) { return { signal: signal, source: signal.sourceType || (registry && registry.getSignalSource(signal.id)) || 'core' }; }); }; var entries = debug ? debug.measure('dictionary.index', buildEntries) : buildEntries(); var buildSearchIndex = function () { return entries.map(function (entry) { var signal = entry.signal; return [signal.term, signal.displayTerm, signal.meaningEn, signal.meaningZh, signal.category, signal.platforms, signal.tone].map(function (value) { return Array.isArray(value) ? value.join(' ') : value || ''; }).join(' ').toLowerCase(); }); }; var searchableIndex = debug ? debug.measure('dictionary.searchIndex', buildSearchIndex) : buildSearchIndex();
  function record(entry) { return progress[entry.signal.id] || null; } function mastery(entry) { var value = record(entry); return value && value.mastery ? value.mastery : 'unseen'; } function label(value) { return value === 'unseen' ? 'Unseen' : value ? value.charAt(0).toUpperCase() + value.slice(1) : 'Unseen'; } function setText(selector, value) { var element = document.querySelector(selector); if (element) element.textContent = value; } function element(tag, className, value) { var node = document.createElement(tag); if (className) node.className = className; if (value !== undefined) node.textContent = value; return node; }
  function updateCounts() { setText('[data-dict-count="total"]', String(entries.length)); setText('[data-dict-count="learned"]', String(entries.filter(function (entry) { var current = record(entry); return current && current.firstLearnedAt; }).length)); setText('[data-dict-count="favorites"]', String(entries.filter(function (entry) { var current = record(entry); return current && current.favorite === true; }).length)); setText('[data-dict-count="personal"]', String(entries.filter(function (entry) { return entry.source === 'personal'; }).length)); }
  function categoryOf(entry) { return entry.source === 'personal' ? 'Personal' : entry.signal.category; }
  function getFiltered() { return entries.filter(function (entry, index) { if (sourceFilter !== 'all' && entry.source !== sourceFilter) return false; if (query && searchableIndex[index].indexOf(query) === -1) return false; if (view === 'community' && filter !== 'all' && categoryOf(entry) !== filter) return false; if (view === 'mastery' && filter !== 'all' && mastery(entry) !== filter) return false; if (view === 'favorites' && !(record(entry) && record(entry).favorite === true)) return false; if (view === 'personal' && entry.source !== 'personal') return false; return true; }); }
  function renderFilters() { sourceArea.textContent = ''; [['all', 'All'], ['core', 'Core'], ['imported', 'Imported'], ['personal', 'Personal']].forEach(function (item) { var button = element('button', sourceFilter === item[0] ? 'is-active' : '', item[1]); button.type = 'button'; button.dataset.source = item[0]; button.setAttribute('aria-pressed', sourceFilter === item[0] ? 'true' : 'false'); sourceArea.appendChild(button); }); sourceArea.querySelectorAll('button').forEach(function (button) { button.addEventListener('click', function () { sourceFilter = button.dataset.source; resetAndRender(); }); }); filterArea.textContent = ''; if (view === 'community') categories.forEach(function (category) { var count = entries.filter(function (entry) { return categoryOf(entry) === category && (sourceFilter === 'all' || entry.source === sourceFilter); }).length; var button = element('button', filter === category ? 'is-active' : '', category + ' ' + count); button.type = 'button'; button.dataset.filter = category; button.setAttribute('aria-pressed', filter === category ? 'true' : 'false'); filterArea.appendChild(button); }); else if (view === 'mastery') [['all', 'All'], ['unseen', 'Unseen'], ['new', 'New'], ['fuzzy', 'Fuzzy'], ['clear', 'Clear']].forEach(function (item) { var button = element('button', filter === item[0] ? 'is-active' : '', item[1]); button.type = 'button'; button.dataset.filter = item[0]; button.setAttribute('aria-pressed', filter === item[0] ? 'true' : 'false'); filterArea.appendChild(button); }); filterArea.querySelectorAll('button').forEach(function (button) { button.addEventListener('click', function () { filter = button.dataset.filter; resetAndRender(); }); }); }
  function makeEntry(entry) { var signal = entry.signal; var current = record(entry); var row = element('div', 'dictionary-entry'); var href = entry.source === 'personal' ? './learn.html?mode=lookup&source=inbox&signal=' + encodeURIComponent(signal.id) : './learn.html?mode=lookup&signal=' + encodeURIComponent(signal.id); var link = element('a', 'dictionary-entry-main'); link.href = href; link.setAttribute('aria-label', 'Open ' + (signal.displayTerm || signal.term)); link.appendChild(element('strong', 'dictionary-term', signal.displayTerm || signal.term)); link.appendChild(element('span', 'dictionary-meaning zh-text', signal.meaningZh || '')); link.appendChild(element('span', 'dictionary-meta', (entry.source.charAt(0).toUpperCase() + entry.source.slice(1)) + ' / ' + categoryOf(entry) + ' / ' + label(mastery(entry)))); row.appendChild(link); var favorite = element('button', 'favorite-button' + (current && current.favorite ? ' is-favorite' : ''), current && current.favorite ? '*' : '+'); favorite.type = 'button'; favorite.setAttribute('aria-label', current && current.favorite ? 'Remove from favorites' : 'Add to favorites'); favorite.setAttribute('aria-pressed', current && current.favorite ? 'true' : 'false'); favorite.addEventListener('click', function (event) { event.preventDefault(); event.stopPropagation(); var existing = progress[signal.id] && typeof progress[signal.id] === 'object' ? progress[signal.id] : { signalId: signal.id, mastery: null, firstLearnedAt: null, lastReviewedAt: null, nextReviewAt: null, reviewCount: 0, errorCount: 0, favorite: false }; progress[signal.id] = Object.assign({}, existing, { signalId: signal.id, favorite: !existing.favorite }); if (storage && storage.setProgress(progress)) { updateCounts(); render(false); } }); row.appendChild(favorite); return row; }
  function render(reset) { if (reset) visibleCount = batchSize; var filtered = getFiltered(); list.textContent = ''; var fragment = document.createDocumentFragment(); filtered.slice(0, visibleCount).forEach(function (entry) { fragment.appendChild(makeEntry(entry)); }); list.appendChild(fragment); setText('[data-results-label]', view === 'community' ? 'By Community' : view === 'mastery' ? 'By Mastery' : view.charAt(0).toUpperCase() + view.slice(1)); setText('[data-results-count]', 'Showing ' + Math.min(visibleCount, filtered.length) + ' / ' + filtered.length + ' signals'); empty.hidden = filtered.length > 0; if (loadMore) { loadMore.hidden = visibleCount >= filtered.length; loadMore.textContent = 'Load 50 more'; } updateCounts(); }
  function resetAndRender() { renderFilters(); render(true); }
  document.querySelectorAll('[data-dictionary-view]').forEach(function (tab) { tab.addEventListener('click', function () { view = tab.dataset.dictionaryView; filter = 'all'; document.querySelectorAll('[data-dictionary-view]').forEach(function (item) { item.classList.remove('is-active'); item.setAttribute('aria-selected', item === tab ? 'true' : 'false'); }); tab.classList.add('is-active'); resetAndRender(); }); }); if (search) search.addEventListener('input', function () { clearTimeout(searchTimer); searchTimer = setTimeout(function () { query = search.value.trim().toLowerCase(); render(true); }, 150); }); if (loadMore) loadMore.addEventListener('click', function () { visibleCount += batchSize; render(false); }); if (debug) { debug.measure('dictionary.categoryCounts', renderFilters); debug.measure('dictionary.initial50.render', function () { render(true); }); } else { renderFilters(); render(true); }
}());
